#include <stdio.h> #include <string.h>  /**  * Struct que representa um território no jogo de guerra  * Contém informações essenciais: nome, cor do exército e número de tropas  */ typedef struct {     char nome[30];    // Nome do território (máximo 30 caracteres)     char cor[10];     // Cor do exército que controla o território     int tropas;       // Quantidade de tropas no território } Territorio;  /**  * Função para cadastrar os dados de um território  * Parâmetros: ponteiro para território e índice (para identificação)  */ void cadastrarTerritorio(Territorio *t, int indice) {     printf("\n--- CADASTRO DO TERRITÓRIO %d ---\n", indice + 1);          printf("Digite o nome do território: ");     fflush(stdin); // Limpa o buffer de entrada     fgets(t->nome, sizeof(t->nome), stdin);          // Remove a quebra de linha capturada pelo fgets     t->nome[strcspn(t->nome, "\n")] = '\0';          printf("Digite a cor do exército: ");     fgets(t->cor, sizeof(t->cor), stdin);     t->cor[strcspn(t->cor, "\n")] = '\0';          printf("Digite o número de tropas: ");     scanf("%d", &t->tropas);          // Validação básica do número de tropas     while (t->tropas < 0) {         printf("Número inválido! Digite um número positivo de tropas: ");         scanf("%d", &t->tropas);     } }  /**  * Função para exibir os dados de todos os territórios cadastrados  * Parâmetros: vetor de territórios e quantidade total  */ void exibirTerritorios(Territorio territorios[], int quantidade) {     printf("\n" "=" * 60 "\n");     printf("           TERRITÓRIOS CADASTRADOS NO JOGO WAR\n");     printf("=" * 60 "\n");          for (int i = 0; i < quantidade; i++) {         printf("\n--- TERRITÓRIO %d ---\n", i + 1);         printf("Nome........: %s\n", territorios[i].nome);         printf("Cor do Exército: %s\n", territorios[i].cor);         printf("Tropas......: %d\n", territorios[i].tropas);         printf("-" * 30 "\n");     }          printf("\nTotal de territórios cadastrados: %d\n", quantidade); }  /**  * Função principal do programa  * Gerencia o fluxo de cadastro e exibição dos territórios  */ int main() {     // Declaração do vetor de structs para armazenar 5 territórios     Territorio territorios[5];     const int TOTAL_TERRITORIOS = 5;          // Cabeçalho do programa     printf("=" * 50 "\n");     printf("    SISTEMA DE CADASTRO DE TERRITÓRIOS - WAR\n");     printf("=" * 50 "\n");     printf("Cadastre %d territórios para iniciar o jogo!\n", TOTAL_TERRITORIOS);          // Laço para cadastrar todos os territórios     for (int i = 0; i < TOTAL_TERRITORIOS; i++) {         cadastrarTerritorio(&territorios[i], i);     }          // Exibição de todos os territórios após o cadastro completo     exibirTerritorios(territorios, TOTAL_TERRITORIOS);          printf("\n🎮 Sistema pronto para iniciar o jogo WAR! 🎮\n");     printf("Pressione Enter para finalizar...");     getchar(); // Pausa para o usuário ver o resultado     getchar(); // Segunda chamada para capturar o Enter final          return 0; }  /*  * EXPLICAÇÃO DA ESTRUTURA DE DADOS:  *   * A struct Territorio é uma estrutura composta que agrupa dados relacionados:  * - nome[30]: array de caracteres para o nome do território  * - cor[10]: array de caracteres para a cor do exército  * - tropas: inteiro que representa a quantidade de soldados  *   * VANTAGENS DE USAR STRUCT:  * 1. Organização: dados relacionados ficam agrupados  * 2. Legibilidade: código mais claro e fácil de entender  * 3. Manutenibilidade: mudanças na estrutura afetam todo o código  * 4. Escalabilidade: fácil adicionar novos campos no futuro  *   * FLUXO DO PROGRAMA:  * 1. Declara vetor de 5 structs Territorio  * 2. Loop for para cadastrar cada território  * 3. Função específica para entrada de dados com validação  * 4. Função específica para exibição formatada dos dados  * 5. Interface amigável com mensagens claras ao usuário  */